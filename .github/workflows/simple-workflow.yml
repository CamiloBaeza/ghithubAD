name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs: 
  steps:
    -uses: actions/checkoout@v2
        
      job1:
        name: primero
      job2:
        name: segundo